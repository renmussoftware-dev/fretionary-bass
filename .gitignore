node_modules/
.expo/
dist/
*.tar.gz
.env
.env.local
# Prebuild output at the project root — we want this ignored, but NOT
# anchored to children like modules/<x>/android which is custom native code
# that must be tracked.
/ios/
/android/

# Claude Code working state — worktrees and local-only project files.
# Worktrees were getting committed as bare gitlinks, which broke the
# Pages workflow's recursive-submodule checkout.
.claude/worktrees/

# Google Play service account (credential — never commit)
google-play-service-account.json

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli